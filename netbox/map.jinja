# -*- coding: utf-8 -*-
# vim: ft=jinja

{#- Start imports as #}
{%- import_yaml tpldir ~ "/defaults.yaml" as default_settings %}
{%- import_yaml tpldir ~ "/osfamilymap.yaml" as osfamilymap %}
{%- import_yaml tpldir ~ "/osmap.yaml" as osmap %}
{%- import_yaml tpldir ~ "/osfingermap.yaml" as osfingermap %}

{%- set defaults = salt['grains.filter_by'](default_settings,
    default='netbox',
    merge=salt['grains.filter_by'](osfamilymap, grain='os_family',
      merge=salt['grains.filter_by'](osmap, grain='os',
        merge=salt['grains.filter_by'](osfingermap, grain='osfinger',
          merge=salt['pillar.get']('netbox:lookup', default={})
        )
      )
    )
) %}

{#- Merge the netbox pillar #}
{%- set netbox = salt['pillar.get']('netbox', default=defaults, merge=True) %}
